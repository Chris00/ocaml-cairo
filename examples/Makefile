ROOT=..
include $(ROOT)/Makefile.conf

# Does not require that the library is installed to compile the examples:
OCAMLINCLUDES = ../src
LIBS_CMXA = bigarray.cmxa cairo.cmxa

default: hello.com


include $(ROOT)/Makefile.ocaml
